// exercise no 13 
let tranportation: string [] = ['civic', 'bike', 'bus', 'car', 'suzuki', 'train' ]
for (let i = 0; i< tranportation.length; i++){
    console.log('I WOULD LIKE TO OWN A '  + tranportation[i]);
}