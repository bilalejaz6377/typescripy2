// exercise no 13 
var tranportation = ['civic', 'bike', 'bus', 'car', 'suzuki', 'train'];
for (var i = 0; i < tranportation.length; i++) {
    console.log('I WOULD LIKE TO OWN A ' + tranportation[i]);
}
